export class estruturaDaNota {

    constructor(date, title, description) {

        
        this.date = date,
        this.title = title,
        this.description = description,
        this.stateComplete = false
    }
}